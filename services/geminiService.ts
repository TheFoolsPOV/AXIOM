
/**
 * Note: AI services are disabled per user preference for local connectivity.
 * This file remains for structure but is not utilized by the core 'Architect Hydrator' features.
 */

export const analyzeApiInsights = async (url: string, method: string, responseData: any) => {
  return "Analysis disabled (Local mode).";
};

export const generateCSharpModel = async (json: any) => {
  return "// C# Model generation via AI is currently disabled.";
};
